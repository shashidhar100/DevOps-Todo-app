pipeline{
    agent any
    stages{
        stage("git checkout"){
            steps{
                echo "first pipeline testing with webhook"
                cleanWs()
            }
        }
    }
}